# ttctl GUI has no third-party dependencies — it uses only the Python standard
# library (tkinter ships with CPython). If `import tkinter` fails on a machine,
# install the OS package, e.g. on Debian/Ubuntu:  sudo apt install python3-tk
